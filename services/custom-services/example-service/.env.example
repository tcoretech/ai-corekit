# Example Service Configuration
# ------------------------------

# Docker image to run
SERVICE_IMAGE='nginx:alpine'

# Port the container listens on
SERVICE_PORT='80'

# Host port to expose (leave blank to not expose externally)
SERVICE_HOST_PORT='8080'
