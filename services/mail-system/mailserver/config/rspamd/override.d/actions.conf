# Override reject action so spam is tagged, not bounced.
# Messages scoring above add_header will get X-Spam headers;
# Dovecot sieve can then file them into Junk.
reject = 9999;
add_header = 6;
greylist = 4;
