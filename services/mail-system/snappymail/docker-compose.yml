services:
  snappymail:
    image: djmaze/snappymail:latest@sha256:5e3d990438809a8a49f8ac5758db03e858e6e9fc0e369e1f9e474f7664079905
    container_name: snappymail
    profiles:
    - snappymail
    restart: unless-stopped
    volumes:
    - snappymail_data:/var/lib/snappymail
    - ${PROJECT_ROOT}/services/.shared-resources/shared:/data/shared
    env_file:
      - .env
    environment:
    - UPLOAD_MAX_SIZE=25M
    - PHP_MEMORY_LIMIT=128M
    - TZ=${TZ:-UTC}
    expose:
    - '8888'
    - '9000'
    healthcheck:
      test:
      - CMD
      - wget
      - -q
      - --spider
      - http://localhost:8888/
      interval: 30s
      timeout: 10s
      retries: 3
volumes:
  snappymail_data: {}
