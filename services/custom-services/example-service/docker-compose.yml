services:
  example-service:
    image: ${SERVICE_IMAGE:-nginx:alpine}
    container_name: example-service
    restart: unless-stopped
    env_file:
      - .env
    expose:
      - "${SERVICE_PORT:-80}"
